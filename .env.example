# LLM 可选：首次运行且 SQLite 无配置时，程序会在命令行交互式询问 API Key、Base URL、Model 并写入 SQLite。
# 若不想交互，可在此填写；Base URL 只填到 /v1，不要加 /chat/completions。
# OPENAI_API_KEY=
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_MODEL=gpt-4o-mini

# 可选：数据库路径，默认 ~/.will/will.db 或 ./will.db
# WILL_DB_PATH=

# 可选：/setup 仅本机可访问；若设 WILL_SETUP_TOKEN 则需带 Bearer Token 或 ?token= 才能访问
# WILL_SETUP_TOKEN=

# Worker 模式或覆盖 DB 时使用
# PORT=3000
# BIND=0.0.0.0
# WILL_MODE=standalone
# FEISHU_APP_ID=
# FEISHU_APP_SECRET=
# FEISHU_ALLOWED_OPEN_IDS=
# WILL_INTERNAL_TOKEN=
# WILL_WORKER_URLS=
